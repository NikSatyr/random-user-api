<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.niksatyr.randomuser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.App"
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.niksatyr.randomuser.ui.activity.MainActivity"
            android:label="@string/users">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.niksatyr.randomuser.ui.activity.DetailsActivity"
            android:label="@string/details" />

        <activity
            android:name="com.niksatyr.randomuser.ui.activity.SettingsActivity"
            android:label="@string/settings" />

    </application>

</manifest>